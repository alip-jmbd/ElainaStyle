
export interface Skill {
  name: string;
  slug: string;
  color: string;
}

export interface SocialLink {
  name: string;
  url: string;
  icon: string;
  color: string;
}

export interface ElainaImage {
  url: string;
  alt: string;
}
